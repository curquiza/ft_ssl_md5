#include "ft_ssl.h"

/* double	pow_double(double v, uint32_t n) */
/* { */
/* 	if (n == 0.0) */
/* 		return 1; */
/* 	return (v * pow(v, n - 1)); */
/* } */

double	abs_double(double x)
{
	return (x < 0 ? -x: x);
}
